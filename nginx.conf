events {
  worker_connections 1024;
}

http {
  server {
    listen 80;

    # Serve React app
    location / {
      root /usr/share/nginx/html;
      try_files $uri /index.html;
    }

    # Proxy API requests
    location /api/ {
      proxy_pass http://server:8000;
    }
  }
}
